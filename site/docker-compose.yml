services:
  sentinel-ops-landing:
    image: nginx:alpine
    container_name: sentinel-ops-landing
    restart: unless-stopped
    ports:
      - "8092:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    labels:
      - "com.sentinelops.domain=seeln.site"
